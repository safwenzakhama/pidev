{% extends 'base.html.twig' %}

{% block title %}Categoryexercice index{% endblock %}

{% block body %}
    <h1>Categoryexercice index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categoryexercice in categoryexercices %}
            <tr>
                <td>{{ categoryexercice.id }}</td>
                <td>{{ categoryexercice.name }}</td>
                <td>{{ categoryexercice.description }}</td>
                <td>{{ categoryexercice.photo }}</td>
                <td>
                    <a href="{{ path('categoryexercice_show', {'id': categoryexercice.id}) }}">show</a>
                    <a href="{{ path('categoryexercice_edit', {'id': categoryexercice.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categoryexercice_new') }}">Create new</a>
{% endblock %}
