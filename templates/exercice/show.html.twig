{% extends 'base.html.twig' %}

{% block title %}Exercice{% endblock %}

{% block body %}
    <h1>Exercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exercice.name }}</td>
            </tr>
            <tr>
                <th>Nbrset</th>
                <td>{{ exercice.nbrset }}</td>
            </tr>
            <tr>
                <th>photo</th>
                <td>{{ exercice.photo }}</td>
            </tr>
            <tr>
                <th>category exercice</th>
                <td>{{ exercice.categoryexercice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exercice_index') }}">back to list</a>

    <a href="{{ path('exercice_edit', {'id': exercice.id}) }}">edit</a>

    {{ include('exercice/_delete_form.html.twig') }}
{% endblock %}
